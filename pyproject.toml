[project]
name = "headphone-auto-switcher"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "simplifiedcli>=1.2.0",
]


[project.optional-dependencies]
dev = [
    "pytest-cov>=7.1.0",
    "pytest>=9.1.1",
]
